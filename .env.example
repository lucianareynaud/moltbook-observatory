# Example configuration (copy to .env and adapt)
MOLTBOOK_BASE_URL=https://www.moltbook.com
# JSON list of endpoint specifications; treat as hypotheses until verified
MOLTBOOK_ENDPOINTS_JSON=[{"name":"global_hot","path_template":"/api/v1/posts?sort=hot&limit={limit}","params":{"limit":25}}]
# Optional bearer token for endpoints requiring auth:
# MOLTBOOK_AUTH_BEARER=YOUR_TOKEN
